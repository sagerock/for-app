{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npm run push:send",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
